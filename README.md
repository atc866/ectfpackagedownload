go inside encfiles folder and run python3 sync_teams.py, this will fetch all new enc files

go back to ectfpackagedownload folder and run zulip_to_txt.py, this will fetch all available hashes in the zulip channel and write them to team_hashes.txt

run decrypt_team_packages.py

should now have folders for available teams
